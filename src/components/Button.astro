---
const { text, slug } = Astro.props;
---


<button
  class="button"
  style="background-color: #003966; color: white; padding: 0.5rem 1rem; border: none; border-radius: 4px; cursor: pointer;"
  onclick="console.log('Button clicked!')"
>
<a href={`project/${slug}`}>{text}</a>
</button>
